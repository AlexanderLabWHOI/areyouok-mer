__default__:
    system: 'slurm'
    account: akrinos
    time: 300 # in minutes
    mem: 100 # in GB
    threads: 5
    queue: compute
    nodes: 1
    command_options:
        slurm:
            command: "sbatch --parsable --qos=unlim"
            key_mapping:
            name: "--job-name={}"
            threads: "-n {}"
            mem: "--mem={}g"
            account: "--account={}"
            queue: "--partition={}"
            time: "--time={}" 
            nodes: "-N {}"
